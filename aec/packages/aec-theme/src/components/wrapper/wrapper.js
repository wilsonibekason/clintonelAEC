import React from "react";
import { connect, useConnect } from "frontity";

const ComponentWrapper = ({ children }) => {
  return <div>ComponentWrapper</div>;
};

export default connect(ComponentWrapper);
